{
  "name": "first",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "cloudinary": "^2.8.0",
    "hono": "^4.10.6",
    "remove": "^0.1.5"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
